// Esquema mínimo para F1 (sin modelos de dominio). Migraciones en fases posteriores.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// Placeholder para generar el cliente Prisma en F1. Sin migraciones en esta fase.
model SchemaPlaceholder {
  id String @id @default(uuid())
}
