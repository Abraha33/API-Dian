## Epic padre

Parte de **#16** — validación del workflow IA-asistido (checklist v2).

## Tu misión

**Speedrun de comandos:** recorrer los 5 guiones del día a día (nuevo, retomar, debug SQL, cierre, sub-ticket).

## Checklist — §8 Casos de uso aplicados como commands

- [ ] **Ticket nuevo:** crear issue → rama → `/start-ticket` → implementar → evidencia → PR → cierre.
- [ ] **Retomar ticket:** `git checkout <rama>` → `/resume-ticket` → continuar → push.
- [ ] **Debug SQL:** error real → `/debug-sql` → fix → `supabase db push` → verificar.
- [ ] **Cierre de ticket:** `/close-ticket` → comentario en issue → PR → calibración.
- [ ] **Sub-ticket:** issue padre → sub-issue → rama propia → PR independiente → cierre.

## Rama sugerida

`feature/qa/<n>-wkf06h-commands-speedrun` (sustituir `<n>` por el número de **este** issue).

## Prueba de cierre

| Qué ejecutar | Resultado esperado | Tiempo |
|--------------|-------------------|--------|
| Checklist §8 marcada con **al menos 3** guiones ejecutados de verdad (los demás pueden quedar N/A justificado) | Flujos coherentes con `docs/workflow.md` | **M** (< 15 min) |

**Evidencia:** lista de qué guiones corriste + enlaces a issues/PRs de prueba si aplica.
