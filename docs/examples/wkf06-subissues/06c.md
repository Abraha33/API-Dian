## Epic padre

Parte de **#16** — validación del workflow IA-asistido (checklist v2).

## Tu misión

Convertir el **Project board** en el reality show del trabajo: crear, etiquetar, mover y evidenciar issues sin perder el hilo.

## Checklist — §3 GitHub Projects y tickets

- [ ] El Project board existe con columnas correctas (Backlog, In Progress, In Review, Done).
- [ ] Se puede crear issue usando template de `.github/ISSUE_TEMPLATE/`.
- [ ] El issue queda con labels correctos (`role-*`, `type-*`, `size-*`, `priority-*`, `module-*` si aplica).
- [ ] El issue se puede mover entre columnas del board.
- [ ] Se puede actualizar un issue (añadir comentario de evidencia).
- [ ] Un issue puede dividirse en sub-issues con sus propias ramas `feature/<rol>/<id>-slug`.
- [ ] **Cada issue tiene una "Prueba de cierre" concreta:** qué ejecutar, resultado esperado, tiempo (S/M).
- [ ] Al cerrar el ticket, el tiempo real de la prueba se compara con el estimado y se anota en la tabla de calibración.

## Rama sugerida

`feature/qa/<n>-wkf06c-projects` (sustituir `<n>` por el número de **este** issue).

## Prueba de cierre

| Qué ejecutar | Resultado esperado | Tiempo |
|--------------|-------------------|--------|
| Crear issue de prueba → labels → mover columnas → comentario evidencia → cerrar o dejar en Done | Flujo completo sin atajos manuales raros | **M** (< 15 min) |

**Evidencia:** enlace al issue de prueba o número + nota del movimiento en el board.
