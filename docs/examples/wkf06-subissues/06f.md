## Epic padre

Parte de **#16** — validación del workflow IA-asistido (checklist v2).

## Tu misión

**Migraciones sin pesadillas:** crear, aplicar, ver en Studio, introspección al día — y `/debug-sql` con error real.

## Checklist — §6 SQL, migraciones y Supabase

- [ ] `supabase migration new <nombre>` crea el archivo en `supabase/migrations/`.
- [ ] SQL editado y aplicado con `supabase db push` sin errores.
- [ ] Tabla visible en Supabase Studio tras el push.
- [ ] `scripts/introspection/current-public-schema.sql` actualizado tras la migración.
- [ ] `/debug-sql` funciona con un error real de migración (caso de uso comprobado).

## Rama sugerida

`feature/qa/<n>-wkf06f-sql-supabase` (sustituir `<n>` por el número de **este** issue).

## Prueba de cierre

| Qué ejecutar | Resultado esperado | Tiempo |
|--------------|-------------------|--------|
| `migration new` + `db push` + verificación Studio + introspección + (opcional) error provocado y `/debug-sql` | Flujo documentado de punta a punta | **M** (< 15 min) |

**Evidencia:** comandos, nombre del archivo de migración, y nota del fix si hubo error intencional.
