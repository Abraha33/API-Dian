## Epic padre

Parte de **#16** — validación del workflow IA-asistido (checklist v2).

## Tu misión (modo speedrun)

Dejar la máquina y las herramientas en **estado “puedo trabajar ya”**: cero fricción al abrir el repo.

## Checklist — §1 Work Environment

- [ ] Repo clonado y rama `dev` actualizada.
- [ ] Supabase CLI instalado y `supabase start` sin errores.
- [ ] Cursor instalado con `project.mdc` cargado y reconocido.
- [ ] Los 4 commands visibles en Cursor (`/start-ticket`, `/resume-ticket`, `/debug-sql`, `/close-ticket`).
- [ ] Archivo `Rama` eliminado de raíz (si aún existe).

## Rama sugerida

`feature/qa/<n>-wkf06a-entorno` (sustituir `<n>` por el número de **este** issue).

## Prueba de cierre

| Qué ejecutar | Resultado esperado | Tiempo |
|--------------|-------------------|--------|
| `git checkout dev && git pull` + `supabase start` + abrir Cursor y ver los 4 commands | Todo OK sin errores bloqueantes | **S** (< 5 min) |

**Evidencia:** comentario en este issue con comandos y resultado (OK / error resuelto).
