# Prompt base — Cursor (tareas en repo)

Úsalo al abrir un chat nuevo o al delegar un cambio concreto. Mantén el mensaje **corto**; adjunta solo archivos necesarios.

---

Trabajas en el repo **API-Dian**, rama de integración **`dev`**. Ramas de trabajo: `feature/...` desde `dev`. Lee y respeta `README.md`, `ROADMAP.md` y `ADR/ADR-001-stack-tecnologico.md` (stack en borrador hasta T0.0.1).

**Tarea:** (una oración)

**Restricciones:**

- No refactors fuera de alcance.
- Esquema DB: solo vía `supabase/migrations/` cuando toque base de datos.
- Documentación en `docs/` en español si creas texto para humanos.

**Archivos o rutas relevantes:** (opcional)

**Criterio de hecho:** (qué debe quedar funcionando o documentado)
